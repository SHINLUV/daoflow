# DaoFlow 高级感优化交付

已直接更新本机原项目，预览地址：http://127.0.0.1:3100/?v=polish3

## 本次设计
以用户提供的《调研前端高级感要素》完整会话正文为依据，保留沙色、松苔绿、轻宋体与山水意象，重点落实层级、内容关系、可读性、组件状态和多端完成度。

| 报告要素 | 已完成的调整 |
|---|---|
| 信息层级 | 首页继续以写下困惑为主要操作；主题区改为主题列表与对应篇章预览，弱化重复卡片的视觉竞争 |
| 空间与分组 | 桌面左右分栏保持统一对齐，手机主题使用两列选择区；关于区以文字、分隔线和品牌释义组织内容 |
| 中文排版 | 统一宋体标题和界面正文字体，提高辅助文字字号；新文案已纳入本地字体子集 |
| 色彩 | 加深小字号苔绿文字，统一选中状态、焦点轮廓和按钮；修正文字选中颜色 |
| 素材与内容 | 新增 AI 生成的水石意象，首页山水与主题水石各有用途；关于区改成排版表达，减少重复图片 |
| 交互状态 | 六个主题都有明确选中状态、原文出处、篇章入口和提问操作；复制反馈自动复位；导航标记对应锚点 |
| 工程完成度 | 保留电脑、手机和窄客户端适配，检查键盘操作、长问题、减少动态效果偏好、错误恢复 |

六个主题的原文分别来自第 16、8、44、64、33、48 章，已与项目内的 81 章原文逐项核对。主题启发是固定编辑文案；个性化回答仍由原有问道流程生成。

## 图片
水石图片由 AI 生成，原图约 1.88 MB；上线使用保持原始尺寸的 WebP 版本，101,758 字节，体积减少约 94.6%。预先完成编码，避免本机现有图片优化环节首次处理该图时长时间等待。图片按屏幕裁切，主体石头保持可见。

原图和网页版本均在 public 中，未引入新的图像处理运行时依赖。

## 最快、最稳、最省钱
以下为同类优化工作量估算，本次已直接交付，无需另选工具或方案。

| 目标 | 方案 | 开发时间估算 | 维护成本 | 风险等级 |
|---|---|---|---|---|
| 最快 | 沿用现有 Next.js、组件与接口，只调整布局和视觉 | 约 1 小时 | 低 | 低 |
| 最稳 | 在现有实现上完成内容核对、响应式和交互验证（本次采用） | 约 1–2 小时 | 低 | 前端低；外部服务仍有原有依赖 |
| 最省钱 | 复用原开源依赖，图片预先编码，本地预览 | 约 1 小时 | 无新增前端订阅费 | 低；AI 生图与原有模型调用按账户计费 |

不迁移框架，不增加建站 SaaS，不重复实现认证、路由或 AI 调度。

## 成熟方案复用
下面沿用前次本项目调研记录；Star 是 2026-09-05 的查询值，不是实时值。

| 项目 | Star | 许可证与维护记录 | 优势 | 劣势 | 适用场景 |
|---|---:|---|---|---|---|
| [Next.js](https://github.com/vercel/next.js) | 142,098 | MIT；9 月 5 日有推送及议题更新 | 原项目已使用，路由、构建、字体与图片能力齐备 | 比静态页有更多构建和版本维护工作 | 本项目这种内容阅读与接口结合的网站 |
| [Motion](https://github.com/motiondivision/motion) | 33,493 | MIT；9 月 2 日有推送及议题更新 | 兼容项目现有动效 | 简单动画使用它可能增加不必要的 JS | 已有组件继续兼容；首页轻量动效使用 CSS |

实现同时沿用 [Next.js 官方 CSS 能力](https://nextjs.org/docs/app/getting-started/css)；没有新增 npm 运行时依赖，也没有升级原框架版本。

## 验证及边界
- 生产构建及其类型检查通过。旧 TaijiSymbol 组件仍有原有 img 优化提示，不阻止构建。
- 原有 Vitest：19 项通过。
- 浏览器：50 项通过，见 DaoFlow-polish-verification.json。覆盖图片实际加载、六主题状态与原文出处、预填聚焦、复制、问道失败重试、阅读、导航、键盘、长输入、减少动效、404。
- 布局：14 种视窗通过，宽度从 320 到 2560 像素。检查横向溢出、标题宽度、图片文字与暂停按钮间距，以及桌面提问框完整可见。
- 加测 900 像素账户页、390 像素阅读页；用 640 像素 CSS 视窗检查 1280 像素窗口在 200% 缩放下的等效重排。这是等效视窗测试，不是真实浏览器缩放测试。
- 基础文字与沙色背景的对比度：主文字 8.67:1、辅助文字 4.79:1、苔绿文字 5.17:1。这是指定颜色组合核对，不代表已完成全站无障碍认证。
- 问道成功、失败和重试采用受控 API 响应测试；未验证真实模型供应商输出。没有发送登录邮件，也没有验证登录后历史同步。
- 没有运行 Lighthouse 或测量真实用户数据；不宣称改版提升转化率。
- 当前仅本机预览，没有发布公网。

## 交付文件
- DaoFlow-source.zip：更新的项目源码、字体、图片、锁文件、配置和原有章节内容。不含密钥、node_modules、.next。
- DaoFlow-desktop.png / DaoFlow-full-page.png：桌面首屏与完整页面。
- DaoFlow-mobile.png：手机完整页面。
- DaoFlow-themes-preview.png：桌面主题区。
- DaoFlow-responsive-checks.json / DaoFlow-polish-verification.json：布局与交互检查记录。

原项目：C:\Users\70719\Desktop\项目\daoflow\daoflow-app

本次修改前备份：C:\Users\70719\Documents\Codex\2026-09-05\qi\work\daoflow-before-polish\src

如果本地预览停止，在原项目目录运行 npm run build，再运行 npm run start -- --hostname 127.0.0.1 --port 3100。
