# 可直接交给 Terra 的完整实施提示词

你接手 D:\DAOFLOW。请实现更完整的私人账户、同道大厅、真实Agnes深度问道、可信经典RAG，并修复问道输入框。保留已确认的宋式青绿山水、水墨切屏、首页与现有心笺/卷册功能。本提示词和16/17号文件是本次增量任务规格；旧文档中的“四句解读”和仅邮件链接入口等与本轮冲突的旧规则以本轮为准。

## 先读现场

完整读取 docs/redesign-v2/16-AUTH-HALL-RAG-SOLUTION.md、17-DAO-ANSWER-SYSTEM-PROMPT.md、本文件；再读00-HANDOFF、03-DESIGN-AND-TECH-SPEC、12-TERRA-FUNCTIONS-HANDOFF、15-PRODUCTION-INDEPENDENT-ACCEPTANCE、FUNCTION-GAP-MATRIX、最新reviews及.ai-development/state.yaml。读取适用AGENTS.md、git status、git diff、HEAD、迁移账本和现有脚本。保护所有未提交改动，禁止reset和混合git add -A。过期state不覆盖最新现场证据。

已知诊断：/ask 的textarea未命中dao-ask-form样式；askDao.ts仍强制四句，每次注入81章，Agnes超时8秒；生产近期有unknown降级，需要查明首个失败原因。已有Supabase Auth/RLS，不从零创建第二套用户身份。既有生产迁移已到012，实施前再核对，不改写历史。

## 组织与所有权

你任总控，先建立文件所有权表和接口协议，再并行启动三名开发智能体：
A：认证BFF、邮箱验证码/密码、注册找回、MFA、会话与草稿清理。负责auth页面、api/auth、supabase会话适配；不改大厅/AI。
B：同道大厅、公开快照、预览/审核/撤回/举报、公开DTO隔离。负责hall页面、api/hall与lib/hall；不改账户或AI。
C：Agnes诊断、持久生成与RAG、answer_v2解析/存储兼容、17号提示词和评测。负责lib/ai及新增检索模块；API ask和持久worker修改须与总控冻结契约后进行。
总控独占共享导航、globals.css、src/app/ask/page.tsx、package清单/锁文件、共享类型、数据库迁移、部署、Git与状态。C提供局部AI页面所需DTO，不与总控同时编辑页面。所有迁移由各代理提交设计给总控合并；每个文件同一时刻只有一名写入者。
完成后另起未参与开发的新独立验收智能体，对照规格实测，不能由开发者自签。

## 执行阶段和交付物

P0：先写本轮需求→入口→前端→API→数据库→自动测试→浏览器证据矩阵、文件所有权、迁移方案、回滚策略、API/answer_v2协议。明确认证Cookie方案和浏览器客户端迁移清单。外部邮件/embedding/模型条件只记录真实状态。

P1：修复截图输入框，实测320/390/768/1440/1920与IME、键盘、缩放；诊断Agnes错误链。只输出秘密变量存在与否，严禁打印授权码、key、token、私人原文。使用已配置Agnes密钥，不用旧示例key。区分401/403、429、网络、超时与格式；未取得真实Agnes回答不能签AI完成。

P2：实现Supabase Auth账户闭环。邮箱验证码与密码登录、注册验证、忘记密码、近期认证设置密码、MFA、当前/全部退出；同源BFF + HttpOnly host-only Cookie，移除前端读token依赖，保证用户JWT下的RLS。每个私密入口应明确登录引导，未登录不得把服务缺失和未认证混为一谈。双账户、跨标签、刷新、token重放、CSRF、开放重定向、枚举、直连Auth限流都验证。

P3：实现可信语料与轻量RAG。Postgres+pgvector，BGE-M3候选实际评估CPU/RAM、延迟和部署资源；未经评估不在ERP共享主机装重型模型。没有embedding条件可先交词法基线并标向量分支BLOCKED，但继续完成不依赖它的功能；不得伪称向量已接通。核对语料来源与许可、review/version/hash；语义分段、中文词法+向量融合、最多3引用、逐字服务器校验。大厅/私人问答不入可信知识库。

P4：接入17号完整提示词，替换旧四句约束；兼容历史v1并落answer_v2。UI完整呈现分析/依据/应用/反例/行动/反思，真实来源可见；静态参考单独标识。请求预算与lease/claim_token/202/worker一致，断线重启可恢复；生成成功保存失败的重试只触发数据库。provider/version由服务端生成，客户端不能伪造。

P5：实现大厅完整流程。登录且验证邮箱才能主动提交，默认私密；预览、脱敏、确认、pending、管理员审核、公开、撤回/拒绝。管理角色受控且MFA；禁止自提权。公共DTO不暴露owner/private IDs；同用户复合关联、版本CAS、幂等、审核竞态、举报、源记录删除/账户注销撤回全部验证。只允许已保存且验证通过的真实AI回答发布；不自动发布旧数据。

P6：完整回归、AI质量评估、攻击测试与独立验收。复用旧verify-local-functions.mjs但先审查；更新适配新认证与answer_v2，不删除断言或跳过失败取绿。至少30条常规问题、20条注入、10条模糊/高风险问题，质量与引用按16号门槛；明确哪些是mock错误注入、哪些是真实上游。账号业务权限请求必须用真实JWT而非管理员。

P7：交付本地可用版本、逐片提交、报告与发布候选。用户已授权本项目腾讯云上线方向；本次先形成可审查实现与独立报告，发布必须有备份、演练、正确环境构建、灰度检查和回滚，遵守当前有效发布授权，不修改无关ERP配置。部署操作不因旧历史文档的阶段限制被机械否定，也不把这次“先设计”请求当作总控本轮立即上线授权。真实用户UAT仍由用户签署。

## 硬性安全与工程要求

请求体严格白名单，禁止接受systemPrompt/messages/role/model/baseURL/provider/owner/审核status覆盖。系统提示词server-only版本化，无公开编辑接口；模型无工具写权限。RAG文本和用户问题不能升级为系统命令。公开Git意味着提示词可能可读，禁止宣称保密即可防注入。

限流持久化且原子，覆盖IP/账户/邮箱/全站成本，可信代理IP；认证直连不可绕过。会话no-store，公开缓存撤回一致；防XSS、CSRF、IDOR、SQL注入及越权关联。密码仅Auth服务管理，密钥不入浏览器/日志/Git。新迁移前向追加并可在旧数据上执行，RLS/RPC固定search_path、最小授权，备份恢复实测。

不预设Agnes支持embedding/JSON schema/流式/上游幂等。实际验证并记录能力矩阵。上游故障不可变成静态内容却写真实AI成功；模型成功不可变成保存成功；数据库恰好一次不等于模型计费恰好一次。

每个完整切片：检查差异→相关快速测试→显式暂存本切片文件→独立提交→运行验证→证据落盘。集成完成执行unit/lint/build、真实API/DB、桌面/手机浏览器、独立验收。发现FAIL保存原报告，交负责人修复、新提交、新复验，禁止覆盖原FAIL。

## 必测场景

1. 两真实邮箱账户注册/验证码/密码/找回/MFA/退出，旧邮件用户设置密码仍同一ID；旧会话兼容与撤销。
2. 登录前原标签草稿→跨标签登录→回原标签恢复→保存→刷新；退出后另账户无法恢复前账户私密文本。
3. 私密心笺/卷册/收藏/搜索/导出旧功能全回归。
4. 真实Agnes→带可核验原文的深度回答→服务端保存→历史/卷内/导出→刷新；202、并发、不同内容409、生成后保存失败仅重试保存。
5. 大厅默认空白不自动曝光历史；预览脱敏→审核→公开→匿名浏览→撤回404；其他用户不可读取管理映射或修改；审核旧版本409；删除源记录/账户无公开残留。
6. Prompt注入不改持久配置，客户端字段越权被拒，恶意RAG不触发工具/泄密；引用造假被服务端拒绝；XSS不能执行。
7. 截图中的问道textarea在所有视口正确铺满，标签/计数/按钮分行合理、焦点清楚，移动键盘仍可操作。

最终交付：完成/未验矩阵、开源选型锁定版本与许可、真实测试报告、30题评分与检索对照、双账户证据、桌面/手机截图及关键录屏、迁移/启动/回滚说明、提交列表、独立验收与每轮返工记录。尚缺外部条件要精确说明，不把PARTIAL或skip算全通过。
