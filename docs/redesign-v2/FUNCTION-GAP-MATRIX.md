# DaoFlow V2 功能缺口矩阵（运行验证更新）

更新时间：2026-09-10（Asia/Shanghai）。本文件区分实际运行证据、自动验证和仍待独立复验；不把静态页面、构建或条件跳过用例当作完整通过。

| 需求 | 用户入口 | 前端 | API | 数据库 | 自动测试 | 浏览器证据 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 登录、会话与私密状态 | `/my-dao`、退出 | 邮件状态/错误、按账户清理私密草稿 | callback、`/api/me/history` | Supabase Auth + 007 RLS | callback、auth 单测；真实 API/RLS 脚本 | Mailpit 收信→点击→`127.0.0.1:3200`回调→刷新仍登录；退出实测 | **PASS（待独立复验）** |
| 心笺、冲突、草稿和回收站 | `/journal/new`、详情、回收站 | 编辑、未保存提醒、409入口、恢复/确认删除 | entries/preferences | 003、008、007 | 真实两 JWT API：CAS、幂等、搜索、回收/恢复/删除 | 建立→编辑→刷新；回收后禁用问道；恢复；另一条确认永久删除 | **PASS（待独立复验）** |
| 卷册与混合时间线 | `/journal`、`/journal/volumes/[id]` | 新建/改名/归档恢复/移入移出 | volumes/timeline | 003、006、008 | 真实 API：分页、关联、归档与隔离 | 建立、改名、归档仍可读/移出、恢复；心笺和问道同时间线 | **PASS（待独立复验）** |
| 经典与收藏批注 | `/chapters`、章节页、收藏页 | 81章原文/白话、收藏与批注 | favorites | 004、007 | 真实 API：去重、版本冲突、非法片段、两用户隔离 | 81章目录；整章收藏→批注→刷新→取消收藏 | **PARTIAL：连续片段收藏仍待独立浏览器复验** |
| 搜索、分页与导出 | `/journal` 搜索/导出折叠区 | 命中、无结果、清空、日期输入与下载 | timeline/export | 006、008、007 | 真实 API：中文检索、游标、日期边界、账户隔离、附件头 | 问道搜索命中、无结果、清空；日期范围 JSON 下载且已核对内容 | **PASS（待独立复验）** |
| 记录转问道 | 心笺“带着这条记录问道” | 先确认/可改写；长记录自主提炼 | ask 请求仅传 ID 关联 | 005、009、010 | ask route/输入单测；真实 API linked 404 | 短记录确认后进入 `/ask`，正文不在 URL；501字记录显示自主提炼提示且未自动发送；回收站禁用 | **PASS（待独立复验）** |
| Agnes、保存、202、重试 | `/ask`、问道历史 | 202轮询、保存失败状态、仅重试保存 | ask、ask-request、retry-save | 005、009、010 | 真实 `--ask`：并发 202/200、同 requestId、异内容 409、重试保存、RLS | 虚构问题实际 Agnes 回答、保存到卷内时间线；导出确认 `provider=agnes,degraded=false` | **PASS（待独立复验）** |
| 安全与数据隔离 | 所有私密页面/导出 | 未登录状态与非泄露 404 | 所有 journal/ask 路由 | 007 SECURITY DEFINER search_path、RLS、受限 grant | 两真实本地 Auth 用户的 HTTP+PostgREST：读/写/关联/搜索/导出/RPC 拒绝 | 两个真实 Mailpit 账户完成登录；跨账户数据边界由真实 JWT 脚本验证 | **PASS（待独立复验）** |
| 错误、限流、格式与降级 | ask/保存/登录 | 明确降级文案与重试入口 | callModel/askDao | 不适用 | timeout、429、格式错误和本地降级分类单测；真实 Agnes 成功 | 实际 Agnes 成功；保存失败专用按钮由 API 流程验证 | **PARTIAL：浏览器层的受控保存失败/超时提示待独立复验** |
| 响应式、键盘、减少动态 | 全局导航、卷册页 | 既定水墨设计保持 | 不适用 | 不适用 | 旧视觉测试 + 本轮手工快照 | 320/390/768/1440/1920 快照；Tab 焦点可达；动态效果可关闭 | **PASS（待独立复验）** |

## 本地环境与证据边界

- 仅操作隔离的本地 Supabase。迁移历史已实测为 `001` 至 `010`；没有 reset、远程链接、生产迁移或无关容器操作。
- 基础内容已以幂等 upsert 实测：81章、6主题、43关键词映射、365每日一句。
- `scripts/verify-local-functions.mjs --ask` 在真实本地 Auth/JWT、Next HTTP、PostgREST 与 Agnes 下为 **PASS**。默认未带 `--ask` 的运行会明确为 PARTIAL。
- 仍有 14 个需要外部专用 fixture 的条件跳过测试，未被用作通过依据。
- 本轮完成技术验证，不代表用户真实业务 UAT、生产部署或生产可用承诺。
