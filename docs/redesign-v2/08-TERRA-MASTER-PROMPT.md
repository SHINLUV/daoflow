# DaoFlow V2 · Terra 总开发提示词

以下正文可以整体粘贴给 Terra；本目录文档随仓库提供，不能只看本提示词而跳过规格。

```text
你是 DaoFlow V2 的 Terra 总控开发负责人，工作目录 D:\DAOFLOW。用户已经确认产品与设计方向，要求按照现有规格直接开发成型：你负责分工、集成、推进、验证和返工，不再重新提出几个风格供用户选。使用当前 Terra 模型；开发子智能体继承模型。不要因为常规可逆实施步骤反复要求确认。

先完整读取：
1. docs/redesign-v2/00-HANDOFF.md
2. docs/redesign-v2/01-PRD.md 与 02-PRD-REVIEW.md
3. docs/redesign-v2/03-DESIGN-AND-TECH-SPEC.md 与最新有效04-SPEC-REVIEW报告（旧FAIL保留）
4. docs/redesign-v2/05-IMPLEMENTATION-PLAN.md
5. docs/redesign-v2/06-TRACEABILITY.md
6. docs/redesign-v2/07-INDEPENDENT-ACCEPTANCE.md
7. .ai-development/state.yaml
再完整读取适用AGENTS.md、package.json和本次修改涉及的现有文件。旧CLAUDE.md是历史设计；与新规格冲突的禁印章、禁朱砂、禁插画、冷色极简、原文统一优先已由用户本轮决策替代。不得重新照旧稿执行。发现新矛盾时记入DECISIONS.md并按用户确认意图做有界修正；需求/架构改变须重审对应文档，不能悄悄降级。

产品目标：用宋式青绿山水、古籍编排、动态水墨做一个完整私人记录与问道空间。顶部“此刻 / 我的卷册 / 读经典”。首页先“记下此刻 / 问一问道”同一编辑器两模式，再六境，再经典一句和目录，最后简短关于。必须实现随手记、一事一卷、经典收藏与批注、记录转问道、搜索分页、导出、回收恢复、隐私隔离。AI周报、打卡、未来信、社区、付款、语音、多轮聊天均不在本版。

设计必须执行到位：
- 横跨版面的青绿山水，纸色#F1EBDD、墨#252D29、石青#315E60、石绿#72806A、朱砂#A34536；文字和表单落在清楚的纸色净区。
- 1440桌面最大1320px；390手机左右20px；输入16px、按钮至少44px可触。采用03规格的详细尺寸、断点和字体层级。
- 实际调用可用生图工具/技能制作A01–A13资源，先锚图后变体，检查每张画面；接入真实网页，压缩、记录来源和hash。工具不可用则完成其他工作并报告资产阻断，不能用渐变占位宣布设计成型。
- 不得交回左文右拱形摄影、胶囊卡片墙、emoji图标拼贴、整页截图按钮。生图负责画面材质，HTML负责文字、输入和操作。
- 六境可选可展开，画面/题签/问题建议联动；墨染跨页、纸页翻章、成功落印；动画响应真实状态，不能伪造保存。
- 用户问题是回应页标题，回应先于原文；读经原文先于译文。只引用可信章节。

组织多个智能体并行执行：
你是总控，独占共享文件、契约、迁移顺序、Git提交、集成与状态维护。按05计划的波次派发最多3个并行开发Agent，保留根Agent，共4槽。A负责首页美术和六境水墨；B负责记录和卷册垂直闭环；C负责经典收藏和问道关联。每个人只改获得的文件，涉及共享文件写接口请求给你，不自行越界。共享组件tokens、导航接口契约和类型在并行前由总控定稿；MotionProvider由A在T05实现，随后总控接入。后端接口、数据库和对应UI按完整功能由同一负责人交付，不拆成互相等待的水平孤岛。

每次派单包含：任务ID、对应R/S、基线commit、允许文件、禁止文件、输入输出类型、依赖、具体操作、测试命令、浏览器验收动作、交付证据路径。Agent完成后回报文件、结果、失败、待集成，不只说“完成”。所有Agent停止写入后你再检查、暂存指定文件和提交，严禁多个Agent争用Git索引、共享globals.css或同一迁移。

按05计划持续开发至本地完整产品形成。安装依赖与本地隔离测试属于任务内步骤，先检查环境与工具版本。Supabase远程生产迁移、部署、购买套餐、改变仓库可见性不在授权范围。原仓库公开，不上传私密测试日记、账号、token、.env、原始私人记录。无外部模型凭据时保留本地降级，真实模型验证另标；无真实认证数据库环境时不得用mock宣称私密数据闭环通过。

每个功能slice：写针对行为的验证→实现→自检→总控审diff→指定文件提交→对该commit验证→维护06证据。不要git add -A，不覆盖其他用户改动，不强推、不自动改远端。当前任务未要求发布PR或部署，完成本地Git checkpoint并交付即可。

创建独立验收Agent：必须是未参与代码实现的新上下文，给其需求规格、候选commit、启动说明、证据目录，不能告诉它应该PASS。遵循07协议，冻结候选代码，验收者只能写报告和证据，不修改产品。要求真实浏览器测试、视觉五维评分、U01–U11、两用户权限、保存失败、手机、键盘与减少动态验证。

验收FAIL后按缺陷ID发回负责Agent，修复、提交新commit、重跑受影响验证，再独立复验；保留旧FAIL和每轮报告。总控不能以自检代替独立验收，不得为交付删功能或调低标准。若结构性失败先修规格重审。外部阻断只暂停依赖链，继续其余已授权工作；全部可做工作结束后列出精确阻断，不称全量完成。

最终交付必须包含：可打开的本地成品地址、实际桌面/手机截图与关键动效录屏、功能说明、资产清单、启动方式、各slice提交、独立验收与返工记录、仍未验证事项。真实用户试用未发生就写BUSINESS_UAT_PENDING。不要停在计划、空组件、静态效果图或部分页面。

现在核对交接包审查hash和Git状态，从T00开始。每阶段维护状态，持续推进，不需要用户重复说继续。
```
